# Inbox

Lines written by hand or by a skill when something shows up that does not deserve an intent yet: a bug, a priority that changes, a template left behind in the repo where a skill was working. One line per entry, `- <YYYY-MM-DD>: <one line>`. A skill that notices something about this repo's harness writes here and carries on with what is there: it does not rerun `/harness-init`, it does not touch scripts, hooks, workflows or `AGENTS.md`, because the fix is made in the harness repo and comes back here by rerunning the phase that owns the file. `/board`, when it arrives, shows them at the opening and asks for each one whether it becomes an intent, a slice with `spec: inbox (<date>)`, or nothing. Until `/board` is there, they are read by hand when a session opens. Closed lines are removed, not ticked off: the history is in git.

- 2026-09-24: the chain has no state between an intent written and a spec wanted, so `docs/intent/later/` and `docs/specs/later/` are a workaround the board honours only because its glob does not recurse; whether that becomes a frontmatter field or a folder the board knows about is for a building session, not now.
- 2026-09-24: `judge.sh` reports `scripts/test-weakening.sh: pass` from the exit status (templates/scripts/judge.sh:284-291), but the script always exits 0, findings included, so the judge is told a weakened PR passed a gate it must not check again.
- 2026-09-24: `docs/spec.md` still names the `Documenti` and `Gate umani` lines of the policy in 2, 5.8, the table of the modes and the Review policy paragraph, where the hooks read `docs_mode` and the heading is "Human gates"; S63 takes only the board strings.
- 2026-09-24: `test-weakening.sh` flags `.skip(` inside a string literal of a test file, so a test of the detector itself gets tests-weakened unless its fixture avoids the literal (S66 built it with `join`).
